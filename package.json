{
  "name": "draw-game",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "wsrun -at build",
    "build:clean": "wsrun -a build:clean",
    "test": "wsrun -a test"
  },
  "engines": {
    "node": "v14.15.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "@packages/*",
      "eslint",
      "jest",
      "react-scripts",
      "typescript"
    ]
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
